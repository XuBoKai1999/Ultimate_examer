# Ultimate_examer

## 目標

Ultimate_examer 是一個泛用的題庫考試與練習 GUI。

程式不應綁定特定考試。使用者只需要更換題庫，即可用於不同考試、科目或語言。

第一版以選擇題為核心。

---

## 題庫

題庫以 `.json` 儲存。

預設題庫位置為：

```text
Bank/
```

一個考試可以只有一個題庫，也可以由多個題庫組成。

GUI 應允許使用者：

* 從 `Bank/` 選取一個或多個題庫。
* 從其他位置自行選取 `.json` 題庫。
* 同時載入多個題庫並共同出題。

程式應盡量保持題庫系統獨立，方便未來擴充其他題型或資料格式。

---

## 考試模式

程式提供三種模式。

### 題目順序

每次開始新的作答 session 時，使用者可以選擇：

* Sequential：依使用者選取題庫的順序組合題目；各題庫的 section 與 question 維持 JSON array 原始順序。
* Random：將所有選中題庫共同形成的題目 pool 隨機排序；同一 session 內每題只出現一次。

三種模式使用相同的順序定義。不提供 seed、加權隨機或 adaptive sampling。

### 題目導航

三種模式都支援 Previous、Next 與題目清單（Jump to Question）。使用者不需要先作答即可離開目前題目，未作答題目保持 unanswered 並可稍後返回。第一題的 Previous 與最後一題的 Next 應 disabled，不循環跳轉。

### 1. Exam Mode

模擬正式考試。

* 使用者設定本次題數。
* 派發題目後不立即顯示答案。
* 完成整場考試後統一批改。
* 顯示得分與答題結果。
* 交卷前可 Previous、Next 或 Jump，題目清單只顯示 answered／unanswered。
* 交卷前不得顯示正確答案或正確／錯誤狀態。

### 2. Practice Mode

逐題練習。

* 題目開始時隱藏答案。
* 使用者選擇答案後立即判定。
* 顯示正確答案。
* 答對與答錯應有明顯視覺區別，例如綠色與紅色。
* 已作答題目的選擇與判定結果在導航後仍須保留，且不可重新作答。
* 題目清單顯示 unanswered、correct 或 incorrect，並可直接跳題。

### 3. Wrong Answer Mode

專門練習過去答錯的題目。

行為基本與 Practice Mode 相同：

* 從錯題紀錄中出題。
* 作答後立即判定。
* 顯示正確答案。
* 提供正確／錯誤視覺回饋。
* 使用與 Practice Mode 相同的 Sequential／Random、導航及狀態保留行為。
* 在 Wrong Answer Mode 答對時，從錯題紀錄移除；答錯時繼續保留，下一個 session 才反映移除結果。

程式需要保存足以支援錯題練習的紀錄。

錯題以 `(bank.id, question.id)` 識別。Practice 與 Exam 答錯時加入，答對不影響既有紀錄；同題不得重複。成功載入 canonical bank 時，可清除該 bank 已不存在的題目 ID，但不得影響未載入的其他 bank。GUI 應能在確認後清除目前所選題庫的錯題，不影響其他 bank。

---

## GUI

GUI 應以簡單、易操作為優先。

至少提供：

* 題庫選擇。
* 模式選擇。
* 題數設定。
* 題目顯示。
* 選項選擇。
* 上一題／下一題或其他合理的題目導航方式。
* 考試結果顯示。
* 字體大小調整。

### 字體縮放

使用者可以放大或縮小 GUI 中主要閱讀內容的字體。

至少支援：

* GUI 控制元件。
* `Ctrl + +`
* `Ctrl + -`
* `Ctrl + 滑鼠滾輪`

縮放行為應自然且不破壞介面。

---

## 語言

GUI 支援中文與英文。

介面文字不應散落硬編碼在各處，應採容易維護與擴充的方式管理。

第一版至少提供：

* 繁體中文
* English

題庫本身的語言不受 GUI 語言限制。

---

## 設計原則

* 保持程式泛用，不綁定任何特定考試。
* 題庫、考試邏輯與 GUI 儘可能解耦。
* 不要為尚未存在的需求過度設計。
* 優先完成簡潔、穩定、可實際使用的版本。
* 保留未來增加題型、統計、題庫格式與其他功能的空間。
* 實作方法與 GUI framework 可依目前 repository 與需求自行選擇合理方案。

---

## 完成標準

第一版完成時，使用者應能：

1. 啟動 GUI。
2. 選擇一個或多個 JSON 題庫。
3. 選擇 Exam、Practice 或 Wrong Answer Mode。
4. 設定合理的出題參數。
5. 正常完成作答。
6. 得到符合模式的答案回饋或考試結果。
7. 將答錯題目納入錯題紀錄並再次練習。
8. 切換中英文 GUI。
9. 使用快捷鍵或 GUI 調整字體大小。
